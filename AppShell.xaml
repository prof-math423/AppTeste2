<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AppTeste.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppTeste"
    xmlns:viewCoin="clr-namespace:AppTeste.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="AppTeste">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Cara ou Coroa"
        ContentTemplate="{DataTemplate viewCoin:CoinView}"
        />
	
	<ShellContent
        Title="Calculadora de Salario"
		ContentTemplate="{DataTemplate viewSalario:SlarioView"
		Route="SalarioView" />

</Shell>
