<Window x:Class="Salario.Views.SalarioView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Salario.ViewModels"
        Title="Calculadora de Salário" 
        Height="220" Width="320">

    <Window.DataContext>
        <vm:SalarioViewModel />
    </Window.DataContext>


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Valor Hora -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,5">
            <Label Content="Valor Hora:" Width="130" VerticalAlignment="Center"/>
            <TextBox Width="140" 
                     Text="{Binding ValorHora, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Horas Trabalhadas -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,5">
            <Label Content="Horas Trabalhadas:" Width="130" VerticalAlignment="Center"/>
            <TextBox Width="140" 
                     Text="{Binding HorasTrabalhadas, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Botão -->
        <Button Grid.Row="2" Content="Calcular" Width="100" 
                HorizontalAlignment="Left"
                Command="{Binding CalcularSalarioCommand}" />

        <!-- Resultado -->
        <TextBlock Grid.Row="3" Margin="0,10,0,0" 
                   FontWeight="Bold" FontSize="16"
                   Text="{Binding SalarioCalculado, StringFormat='Salário: {0:C}'}" />
    </Grid>
</Window>
